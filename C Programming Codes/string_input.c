#include<stdio.h>
int main()
{   
    int i=0;
    char ch;
    char str[100];
    while(ch1='\n'){
        scanf("%d",&ch);
        str(i)=ch;
        i++;
    }
    str(i)='\0';
    puts(ch);
    return 0;
}