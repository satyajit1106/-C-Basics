#include<stdio.h>
#include<math.h>
void main()
{
     long int a = 10;
     long int b = 3;
    // int c = a+b;
    // long long or long is used for the big calculated value

    double power = pow(a,b);

    printf("%f\n",power);
    
    // always use the {%f} in the power statement for the accurate calculation


    int d,e;
    d = e= 2;
    int f = pow(d,e);
    printf("%d\n",f);

    printf("%f",12*1.2);


}

/* 
while using the arithmetic operation in the c language we always define one variable on the left hand side


*/